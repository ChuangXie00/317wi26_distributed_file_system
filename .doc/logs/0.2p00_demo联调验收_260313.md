# 0.2p00 Demo 联调验收记录（Commit 10）

更新时间：2026-03-13（America/Los_Angeles）  
工作目录：`d:\csen317_proj\dfs`  
对应提交目标：8890 第 7 节 `Commit 10`

## 1) 联调环境与前置状态

- Docker 集群：`meta-entry + 3 meta + 3 storage + postgres` 全部运行。
- Frontend：`http://127.0.0.1:5173`（Vite dev server）。
- Demo Backend：使用 `.venv` + FastAPI TestClient 执行联调调用。
- Demo Backend 联调环境变量：
  - `DEMO_META_ENTRY_BASE_URL=http://127.0.0.1:8000`
  - `DEMO_COMPOSE_FILE=deploy/docker-compose.yml`
  - `DEMO_COMPOSE_TIMEOUT_SEC=20`

## 2) 本次验收路径（stop / election / switch / recover）

说明：04 中示例写法为 `stop meta-01`。本次运行时基线 leader 为 `meta-02`，因此按“停止当前 leader”执行，语义与演示闭环一致。

### 2.1 基线观测

- `GET /api/demo/state`
- 基线结果：
  - `leader=meta-02`
  - `entry.active_leader_id=meta-02`
  - `summary={alive:6,suspected:0,dead:0,total:6}`

### 2.2 故障注入（stop）

- `POST /api/demo/action`：`{"action":"stop","target":"meta-02"}`
- 关键回执：
  - `action_id=act_20260313_48acaeb9`
  - `normalized_action=stop`
  - `duration_ms=715`

### 2.3 选举与入口切换（election + switch）

- 轮询 `GET /api/demo/state` 直到满足：
  - 新 leader 不等于旧 leader；
  - `entry.active_leader_id` 与新 leader 一致；
  - 被 stop 节点状态为 `dead/suspected`。
- 实际观测：
  - `new_leader=meta-03`
  - `entry.active_leader_id=meta-03`
  - `old(meta-02).status=dead`

### 2.4 恢复（recover）

- `POST /api/demo/action`：`{"action":"start","target":"meta-02"}`
- 关键回执：
  - `action_id=act_20260313_02b8dbb9`
  - `normalized_action=start`
  - `duration_ms=880`
- 轮询恢复结果：
  - `meta-02.status=alive`
  - `meta-02.role=follower`
  - 当前 leader 保持 `meta-03`

### 2.5 事件流与指标闭环

- `GET /api/demo/events?since_seq=0&limit=200`
  - `events_total=11`
  - `leader_switched=1`
  - `action_events=4`
  - `recovery_completed=1`
- `GET /api/demo/metrics`
  - `status=200`
  - `failover_recovery_seconds=21.608`

## 3) 对照 04（L536-L553）验收项

| 04 条款 | 验证结果 | 证据 |
| --- | --- | --- |
| L538：页面初始展示 3 个 meta，entry 指向 leader | 通过（当前 leader 为 `meta-02`） | `/state` 基线 `meta=3`，`entry=meta-02` |
| L539：stop leader 后新 leader 正常当选 | 通过 | `stop meta-02` 后 `new_leader=meta-03` |
| L540：entry 自动切到新 leader | 通过 | `entry.active_leader_id=meta-03` |
| L541：start 原 leader 后以 follower 回归 | 通过 | `meta-02 status=alive, role=follower` |
| L542：页面展示与 membership/epoch/lamport/event 对齐 | 通过 | `/state` + `/events` 同步变化，事件含 `leader.switched` |
| L548-L553：完整演示闭环 + 事件可追踪 + entry 显式可见 | 通过 | `stop/election/switch/recover` 全链路完成，`/events` 与 `/metrics` 输出有效 |

## 4) 关键执行命令（节选）

```powershell
# 容器状态
docker ps --format "table {{.Names}}\t{{.Status}}"

# 前端构建
cd frontend
npm.cmd run build

# Demo Backend 联调（使用 .venv + TestClient）
cd d:\csen317_proj\dfs
.\.venv\Scripts\python.exe - < commit10_联调脚本(内联)
```

## 5) 结论

- Commit 10 验收路径已跑通：`stop -> election -> switch -> recover`。
- 04 对齐版验收项（L536-L553）在本次联调中均已给出可追踪证据。
